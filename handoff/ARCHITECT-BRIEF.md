# Architect Brief
*Written by Architect. Read by Builder and Reviewer.*
*Overwrite this file each step — it is not a log, it is the current active brief.*

---

## No active step

Steps 5A (recovery ratio report), 5B (per-slab dimension overrides), 5C (item-level Tally
detail), and 5D (Next.js 15→16 upgrade) all cleared review and are merged to `main` as of
2026-07-12. See `handoff/BUILD-LOG.md` for full detail on each, including Step 5D's
dual-React-copy fix.

README's original next-steps list is now fully closed out except for real-data verification on
#6 (Tally item-level import — needs the Owner's own manual run against a real Tally export) and
role-based dashboard views beyond owner/admin (not started, no brief written).

---

## Builder Plan
*Builder adds their plan here before building. Architect reviews and approves.*
